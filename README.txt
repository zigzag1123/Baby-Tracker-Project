To run file make sure to download the CapstoneCopy.py and the 4 .csv files titled; User1, User2, User3, User4
Open command prompt and CD to where the python file and csv files arfe located.
You will need to run these commands prior to running the main file.

pip install pandas
pip install tk
pip install matplotlib

After this you can run the main code with

python Capstonecopy.py